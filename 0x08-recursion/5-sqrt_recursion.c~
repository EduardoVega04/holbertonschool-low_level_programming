#include "holberton.h"

int mysqrt(int x, int y)
{
if (x == (y * y))
return (y);
else if (x > (y * y))
return (mysqrt(x, y + 1));
else
return (-1);
}

int _sqrt_recursion(int n)
{
if (n == 0)
return (0);
else if (n == 1)
return (1);
else if (n < 0)
return (-1);
else
return (mysqrt(n, 1));
}
